{
  "name": "rapido-quote",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Groupe Rapido — calculateur de soumission instantanée (Cloudflare Worker + widget Elementor).",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "npm test && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
